import { RequestHandler } from 'express'

export const validateCreateNft: RequestHandler = (_req, _res, next) => {
  next()
}

export const validateUpdateNft: RequestHandler = (_req, _res, next) => {
  next()
}
