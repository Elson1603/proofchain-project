import { RequestHandler } from 'express'

export const validateCreateAuth: RequestHandler = (_req, _res, next) => {
  next()
}

export const validateUpdateAuth: RequestHandler = (_req, _res, next) => {
  next()
}
