import { RequestHandler } from 'express'

export const validateCreateUser: RequestHandler = (_req, _res, next) => {
  next()
}

export const validateUpdateUser: RequestHandler = (_req, _res, next) => {
  next()
}
