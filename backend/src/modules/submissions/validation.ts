import { RequestHandler } from 'express'

export const validateCreateSubmission: RequestHandler = (_req, _res, next) => {
  next()
}

export const validateUpdateSubmission: RequestHandler = (_req, _res, next) => {
  next()
}
