import { RequestHandler } from 'express'

export const validateCreateChat: RequestHandler = (_req, _res, next) => {
  next()
}

export const validateUpdateChat: RequestHandler = (_req, _res, next) => {
  next()
}
