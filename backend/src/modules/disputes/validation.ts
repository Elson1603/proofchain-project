import { RequestHandler } from 'express'

export const validateCreateDispute: RequestHandler = (_req, _res, next) => {
  next()
}

export const validateUpdateDispute: RequestHandler = (_req, _res, next) => {
  next()
}
