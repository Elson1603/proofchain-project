import { RequestHandler } from 'express'

export const validateCreateNotification: RequestHandler = (_req, _res, next) => {
  next()
}

export const validateUpdateNotification: RequestHandler = (_req, _res, next) => {
  next()
}
