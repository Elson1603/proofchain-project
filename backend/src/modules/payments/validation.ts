import { RequestHandler } from 'express'

export const validateCreatePayment: RequestHandler = (_req, _res, next) => {
  next()
}

export const validateUpdatePayment: RequestHandler = (_req, _res, next) => {
  next()
}
