import { RequestHandler } from 'express'

export const validateCreateProject: RequestHandler = (_req, _res, next) => {
  next()
}

export const validateUpdateProject: RequestHandler = (_req, _res, next) => {
  next()
}
